import { createDebug, createLogger } from '@arcblock/ux/lib/Util/logger';

const debug = createDebug('blocklet-services');
const logger = createLogger('blocklet-services');

export { debug, logger };
